CREATE TABLE AUDIT.ADDRESS_AUD (
    ID BIGINT NOT NULL,
    REV INTEGER NOT NULL,
    REVTYPE TINYINT,
    HOUSE_NUMBER VARCHAR(255),
    STREET VARCHAR(255),
    ZIP_CODE VARCHAR(255),
    PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.ADDRESS_AUD ADD CONSTRAINT ADDRESS_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;