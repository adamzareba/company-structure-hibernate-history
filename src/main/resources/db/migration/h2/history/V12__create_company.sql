CREATE TABLE AUDIT.COMPANY_AUD (
    ID BIGINT NOT NULL,
    REV INTEGER NOT NULL,
    REVTYPE TINYINT,
    NAME VARCHAR(255),
    PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.COMPANY_AUD ADD CONSTRAINT COMPANY_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;