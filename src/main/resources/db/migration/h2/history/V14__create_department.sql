CREATE TABLE AUDIT.DEPARTMENT_AUD (
    ID BIGINT NOT NULL,
    REV INTEGER NOT NULL,
    REVTYPE TINYINT,
    NAME VARCHAR(255),
    COMPANY_ID BIGINT,
    PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.DEPARTMENT_AUD ADD CONSTRAINT DEPARTMENT_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;