CREATE TABLE AUDIT.EMPLOYEE_AUD (
    ID BIGINT NOT NULL,
    REV INTEGER NOT NULL,
    REVTYPE TINYINT,
    NAME VARCHAR(255),
    SURNAME VARCHAR(255),
    ADDRESS_ID BIGINT,
    DEPARTMENT_ID BIGINT,
    PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.EMPLOYEE_AUD ADD CONSTRAINT EMPLOYEE_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;