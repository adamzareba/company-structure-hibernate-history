CREATE TABLE AUDIT.OFFICE_AUD (
    ID BIGINT NOT NULL,
    REV INTEGER NOT NULL,
    REVTYPE TINYINT,
    NAME VARCHAR(255),
    ADDRESS_ID BIGINT,
    DEPARTMENT_ID BIGINT,
    PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.OFFICE_AUD ADD CONSTRAINT OFFICE_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;