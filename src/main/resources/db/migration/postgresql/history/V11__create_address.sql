CREATE TABLE AUDIT.ADDRESS_AUD (
  ID           INT8 NOT NULL,
  REV          INT4 NOT NULL,
  REVTYPE      INT2,
  HOUSE_NUMBER VARCHAR(255),
  STREET       VARCHAR(255),
  ZIP_CODE     VARCHAR(255),
  PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.ADDRESS_AUD
  ADD CONSTRAINT ADDRESS_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;