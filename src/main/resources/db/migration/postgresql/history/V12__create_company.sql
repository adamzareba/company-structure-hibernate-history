CREATE TABLE AUDIT.COMPANY_AUD (
  ID      INT8 NOT NULL,
  REV     INT4 NOT NULL,
  REVTYPE INT2,
  NAME    VARCHAR(255),
  PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.COMPANY_AUD
  ADD CONSTRAINT COMPANY_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;