CREATE TABLE AUDIT.DEPARTMENT_AUD (
  ID         INT8 NOT NULL,
  REV        INT4 NOT NULL,
  REVTYPE    INT2,
  NAME       VARCHAR(255),
  COMPANY_ID INT8,
  PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.DEPARTMENT_AUD
  ADD CONSTRAINT DEPARTMENT_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;