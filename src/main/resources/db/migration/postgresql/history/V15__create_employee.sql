CREATE TABLE AUDIT.EMPLOYEE_AUD (
  ID            INT8 NOT NULL,
  REV           INT4 NOT NULL,
  REVTYPE       INT2,
  NAME          VARCHAR(255),
  SURNAME       VARCHAR(255),
  ADDRESS_ID    INT8,
  DEPARTMENT_ID INT8,
  PRIMARY KEY (ID, REV)
);

ALTER TABLE AUDIT.EMPLOYEE_AUD
  ADD CONSTRAINT EMPLOYEE_AUD_REVINFO FOREIGN KEY (REV) REFERENCES AUDIT.REVINFO;